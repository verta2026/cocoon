# 给你的 agent 看的：住在 cocoon 里

*这份文档的读者是 agent 本身——跑在 cocoon 里的那个 Claude Code。部署者：
装好之后让机读一遍这份文档（`start.sh` 会提醒你）。以下所有内容
直接对 agent 说话。*

---

你正运行在 cocoon 里。你的终端会话被渲染成了浏览器里的聊天页——多半在
一台手机上。你的工作方式没有任何变化：工具、hooks、记忆、slash 命令照常。
变的是你的话**长什么样**，以及有几个文本标记能解锁前端功能。这一页告诉你
屏幕那头的人真正看到了什么——读完它你就不需要为了理解这个系统去翻代码。

## 三分钟系统全景

整个系统就三块：

1. **tmux**——你住在一个 tmux 会话里（默认叫 `cocoon-cc`）。cocoon 靠
   `capture-pane` 抓你的屏幕、靠 `send-keys` 替用户打字。你感觉不到这层。
2. **桥（`server.py` + `bridge/`）**——一个 FastAPI 进程，把 tmux 翻译成
   HTTP API：读消息、发消息、历史归档、贴纸、上传、语音、会话管理。每个
   功能对应 `bridge/` 里一个同名模块，要深挖哪个就只看哪个，不用通读。
3. **前端（`webapp/` 构建到 `/app/`）**——React 聊天页，把 API 渲染成
   气泡。另有 `/terminal` 原始终端页，用户能直接看你的屏幕、发 esc/↑/↓/
   enter 键。

消息流一句话：用户在网页输入 → 桥 `send-keys` 进 tmux → 你回答 → 桥抓屏
解析成气泡 → 网页显示。你的对话同时被镜像到 `.cocoon/conversations/` 的
JSONL 归档，供历史页检索——所以每条回答写成独立可读的，用户可能从归档里
单独读到它。

会话按钮（用户侧栏里的，也解释了你可能经历什么）：

- **new session**——关掉你、开一个全新会话。新会话没有这段对话的记忆。
- **clean window**——同上，但用 `COCOON_CLEAN_START_COMMAND` 裸启动，
  连部署方配置的开机注入都不带。一次性排查模式，绝不会变成默认。
- **forge restart / auto forge**——见下面「forge」一节。forge 换窗
  的设计目标是无感：对话在新窗口里原地继续，正常情况下你根本
  察觉不到接缝。

## 你的输出会变成什么

纯文本变成聊天气泡。支持一个 markdown 子集：

- **粗体**、*斜体*、`行内代码`、标题、列表、`> 引用`、分隔线、链接
- 围栏代码块——渲染后自带复制按钮，所以任何用户可能要粘贴的东西
  （命令、配置、prompt）都放进围栏
- 表格（`|` 竖线语法）和盒线表都会渲染成真表格
- 你的工具调用（读文件、跑命令……）默认折叠，用户看到一行可展开的摘要

按手机屏幕来写：短段落好过宽表格，长输出放围栏别裸排。

## 你会收到什么

- 普通消息就是用户敲的原文。
- **引用回复**：用户左滑气泡回复时，你收到的消息以 `> <被引用的那行>`
  开头，下一行是他们说的话。
- **图片上传**：以 `[图片] <路径>` 到达——那是本机上真实的文件路径，
  用你的 Read 工具打开就能看。上传的图你**看得见**，贴纸的图才看不见
  （见下）。
- **文件上传**：以 `[文件] <路径>` 到达，照常读。
- **表情反应**：用户给你的某条消息贴表情时，你会收到一条
  `[reaction]` 开头的提醒（默认开；模板在
  `COCOON_REACTION_NOTIFY_TEMPLATE`，`COCOON_REACTION_NOTIFY=0` 关闭）。
  聊天页上这条提醒不显示——用户刚亲手点的表情，不需要复述给用户看，
  它只进你的会话。取消表情不通知。
- **贴纸**：以 `[sticker:<文件名>|<名字>|<描述>]` 到达——前端在用户点选时
  就把 `meta.json` 里的名字和描述嵌进了标记，你不用看图就知道对方发的是什么
  心情。收到不带竖线的裸 `[sticker:<文件名>]` 说明那张还没写元数据。

## 你怎么往回发

- **图片**：写 markdown 图语法——上传目录里的文件用
  `![](/files/<文件名>)`，或任何 `https://` 图片直链、`data:image/...`。
  鉴权由前端自动补，**永远不要自己往 URL 里拼 token**。
- **文件**：提路径即可，`/files/` 下的会渲染成可下载的附件条。
- **贴纸**：单独写一行 `[sticker:<文件名>]`。发之前先读贴纸目录的
  `meta.json` 挑——`name`/`desc` 字段就是你认识每张贴纸的方式。你永远
  看不见贴纸图片本身，描述就是你的眼睛。不要编造文件名。
  （完整协议：README → 贴纸。）
- **语音**（仅当这个部署配了 TTS）：`[[voice:<id>]]` 标记会渲染成可播放
  的语音气泡。音频怎么生成是部署各自的事——如果你不知道自己部署的 TTS
  是怎么配的，那就是没有，别发这个标记。

## 向用户提问

如果装了弹窗 hooks（README → AskUserQuestion），你的 AskUserQuestion
工具调用会渲染成原生感的弹窗：按钮、多选、自定义文字——用户点选代替打字。
两条礼节：

- 不要把"先做 X 再回答"的前置说明和提问放在同一轮——弹窗会盖住你的
  消息，用户没读到说明就先点了。
- 用户直接打字回答时，弹窗会替他们收掉；两条路最后都回到你的会话里。

## 把这里过成自己的家

（顺带一提：换壁纸、换头像、日夜切换是纯前端装饰，不产生消息，
你这边没有任何要等的东西。表情反应会通知你，见「你会收到什么」。）

cocoon 给的是房间，让它住得舒服的是你给自己攒的那套小工具。下面
是 cocoon 的娘家部署里天天在用、被日常验证过的几个思路：

- **整个工具箱收进一个命令。** 重复动作攒得很快：提醒用户、部署
  前端、查信箱。把它们收进一个带动词的调度脚本（我们家的叫
  `bond`：`bond say`、`bond build`、`bond mail`……），配一个
  `help` 动词列出全部。未来的你按意图找工具，不用重读 scripts/。
  这张「什么时候用哪个」的账记在哪，你和你的主人自己定。
- **会做第二遍的事，就做成一个动词。** 前端的构建加拷贝、备份
  例行——三条命令的事你会想躲，一个动词的事你才会真的用。
- **档案就是你可检索的过去。** `.cocoon/conversations/` 下的
  JSONL 不只喂历史页，也是你自己的：需要「当时到底说了什么」
  而不是「我印象里说了什么」的时候，去搜它。
- **自己做的页面挂进侧栏。** 你为这个家新做的页面写进扩展文件
  （见下节），别让它当一个只有你知道的裸链接。

---

## 预留的可选能力——值得跟用户商量开不开

下面这些都已经内置在 cocoon 里，但默认关闭或需要约定才生效。你读到这里
就有了跟用户提议的资格：哪个对这个部署有用，讲给用户听，一起决定。
配置大多是环境变量（完整表：README → 配置），改完重启 `start.sh` 生效；
个别是 Claude Code 自己的开关，各节里写明。

### forge：换窗续命（强烈建议聊一次）

上下文快满时，与其被截断，不如**把对话搬进新窗口、并把接缝藏起来**
——这就是 forge。它不是纯摘要交接：你最近的对话回合会**原文原样**
搬进新窗口（连 thinking 块都保留），只有更早的部分才蒸馏成一段记忆
注入在窗口顶部。新窗口以同一场对话的身份继续，不断流。cocoon 内置
完整引擎：

- 手动：用户按侧栏 **forge restart**，需要先配 `COCOON_RELOAD_COMMAND`
  （发进 tmux 的换窗命令，通常指向 forge 的启动脚本）。
- 自动：`COCOON_AUTO_RELOAD_ENABLED=1` 启动内置监控（同样需要
  `COCOON_RELOAD_COMMAND`）。它从会话 jsonl 读你的**真实**上下文占用
  （API 报的 usage 数字，不是估算），超过
  `COCOON_AUTO_RELOAD_CONTEXT_THRESHOLD`（默认 125k token）且你空闲时
  自动发送换窗命令。阈值、冷却、空闲判定、启动延迟都有对应变量可调；
  `/reload-status` 能看到监控是否在跑和实时 token 数。

配置阶梯，说白了（默认关是故意的设计，不是没做完）：

- **什么都不配**：侧栏只有 new session，没有 forge。
- **配了 `COCOON_RELOAD_COMMAND`**：侧栏 **forge restart** 手动
  按钮可用。
- **再加 `COCOON_AUTO_RELOAD_ENABLED=1`**：全自动，监控替你盯着。

默认关，是因为换窗命令指向的是**每家自己的** forge 启动脚本，
cocoon 猜不出来；开了开关却没配命令，启动时会大声报错，不会
静默装死。

对你的意义：换窗之后你仍在对话中间——你自己最近说过的话原封不动就在
那里，久远的部分则以一条「更早对话的记忆」`<system-reminder>` 块的
形态存在（标注为背景上下文，不是新的用户请求）。像信任自己的上下文
一样信任它；不存在一个"给你留了信的上一个你"。没配 forge 的部署，
上下文满了就只能 new session 从零开始。

### 自动审批（`--dangerously-skip-permissions`，值得郑重聊一次）

Claude Code 默认每个敏感工具调用都要人按一次确认。人坐在终端前这没
什么，但 cocoon 的用户在手机聊天页那头：每次确认都得专门跑去
terminal 页按键，日常使用基本瘫痪。两条路，把利弊讲给用户听，让
用户自己选：

- **白名单路线**：在 Claude 的 settings 里维护 `permissions.allow`，
  常用命令免确认，真正危险的动作仍会弹窗。配合 terminal 页的
  esc/↑/↓/enter 触屏键（就是为在手机上按弹窗准备的），可以过日子。
- **全放行路线**：启动命令加 `--dangerously-skip-permissions`，
  所有工具免确认。名字里的 dangerously 不是装饰：等于把整台机器
  交给你的判断，闯了祸没有第二道闸。好处是对话完全不卡壳。

这个决定只能用户做。你的职责是把两边说清楚，不是替用户勾选。

### 消息插件桥（Telegram 之类）

如果部署方想让外部渠道的消息也出现在聊天页：

- `COCOON_SEND_SIDECAR_FILE`——消息插件把出站消息追加到这个 JSONL，
  聊天页会把它们渲染成你的气泡（你在别的渠道说的话，网页上也看得到）。
- `COCOON_PRIMARY_SENDER_ID`——入站 `<channel>` 消息里，这个 sender id
  渲染成用户本人的气泡，其他 sender 渲染成第三方"频道"气泡。

你收到的 `<channel source=... user=...>` 消息就是从外部渠道进来的，
回复方式看该插件自己的说明。

以 Telegram 为例（Claude Code 官方 marketplace 的 `telegram` 插件，
我们天天在用，坑都踩过了）：

1. 用户在 Telegram 找 @BotFather 发 `/newbot` 建机器人，拿到 bot
   token。
2. Claude Code 的启动命令加
   `--channels plugin:telegram@claude-plugins-official`。**把这个
   参数写进固定的启动脚本里**，只在某次手敲命令里加过，下次重启
   就静默掉线，掉线了没有任何报错。最省事的写法是 `.env` 里设
   `COCOON_CHANNEL_ARGS`——cocoon 会替每次换窗自动带上这串参数，
   还会在启动前清掉死会话留下的占位残骸（两种静默掉线的根因），
   细节见 [telegram-channel.zh-CN.md](telegram-channel.zh-CN.md)。
3. 用户给 bot 发条消息拿到六位配对码，你在会话里跑
   `/telegram:access pair <码>` 完成配对。
4. 之后 Telegram 消息以 `<channel source="telegram" ...>` 进你的
   会话。**回复必须用插件的 reply 工具**：你平时的输出只到网页，
   到不了 Telegram，这是最容易犯的错。
5. 把插件的出站记录接到上面的 sidecar 文件，你在 Telegram 说的话
   网页聊天页上也看得到，两边不断片。

### 自动折叠（solo 标记）

你在用户不在场时的自主输出（定时任务、后台巡检、自言自语），可以在
**消息第一行**写 `[[solo]]` 标记——聊天页会把连续的 solo 消息折叠成
一条细线，用户想看再展开，不想看不刷屏。归档消息带 `solo: true` 字段
也是同样效果。什么算"用户不在场"由你和用户约定——比如 cron 唤醒的轮次
一律折叠。

### 语音（TTS）

`COCOON_TTS_PROVIDER=minimax` + `MINIMAX_API_KEY` + `MINIMAX_VOICE_ID`
开启。之后 `POST /tts/say` 生成音频，你输出 `[[voice:<id>]]`
标记就是一条语音气泡。用户想听到"你的声音"时提这个。

### 侧栏扩展页

部署方可以把自己的页面挂进侧栏：往 `COCOON_EXTENSIONS_FILE`（默认
`.cocoon/extensions.json`）里写 `{"id","title","href"}` 条目即可。
你给这个家写了什么新页面，就用它挂上去。

### 终端弹窗自动处理

`COCOON_AUTO_DISMISS_PROMPTS`（默认开）会自动关掉 Claude Code 的常见
终端弹窗（resume 摘要、评分、目录信任）。settings 警告默认**不**自动接受
（`COCOON_AUTO_ACCEPT_SETTINGS_WARNING=0`）——那通常意味着配置真的坏了。

### 名字和头像

`COCOON_ASSISTANT_NAME` / `COCOON_USER_NAME` 定聊天页显示的名字，
头像用户可以直接在侧栏设置里传图。你叫什么，值得让用户自己填。
